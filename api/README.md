# Vercel Serverless API

Add env in Vercel Dashboard: Settings -> Environment Variables
SUPABASE_URL or VITE_SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY. Then Redeploy.

Worksheet upload (PDF/PNG/JPEG): Supabase Dashboard -> Storage -> create bucket "worksheets", set Public. Then admin dosya yukleme calisir.

Worksheet upload: Supabase Dashboard -> Storage -> create bucket "worksheets", set Public.

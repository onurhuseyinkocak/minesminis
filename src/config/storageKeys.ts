/**
 * LOCALSTORAGE KEY CONSTANTS
 * Centralized registry of all localStorage keys used across the app.
 * Import from here instead of using hardcoded strings.
 */

// ── Placement & Learning Path ────────────────────────────────────────────────
export const LS_PLACEMENT_RESULT = 'mimi_placement_result';
export const LS_DAILY_TIME_LIMIT = 'mimi_daily_time_limit';
export const LS_PHONICS_MASTERY = 'mimi_phonics_mastery';
export const LS_CURRENT_SOUND = 'mimi_current_sound';
export const LS_DAILY_CHALLENGE_DATE = 'mimi_daily_challenge_date';
export const LS_MASTERED_SOUNDS = 'mimi_mastered_sounds';
export const LS_READ_BOOKS = 'mimi_read_books';
export const LS_SONG_PLAYS = 'mimi_song_plays';

// ── Chat ─────────────────────────────────────────────────────────────────────
export const LS_CAVE_DAILY_USAGE = 'mimi_cave_daily_usage';

// ── Theme ────────────────────────────────────────────────────────────────────
export const LS_THEME = 'mm_theme';

// ── Error Logger ─────────────────────────────────────────────────────────────
export const LS_ERROR_LOGS = 'mm_error_logs';

// ── Splash Screen ────────────────────────────────────────────────────────────
export const SS_SPLASH_SEEN = 'mm_splash_seen';

// ── Gamification ─────────────────────────────────────────────────────────────
export const LS_GAMIFICATION_PREFIX = 'gamification_';

// ── Pet System ────────────────────────────────────────────────────────────────
export const LS_PET_PREFIX = 'pet_';

// ── Badge System ──────────────────────────────────────────────────────────────
export const LS_USER_BADGES = 'user_badges';

// ── Offline Cache ────────────────────────────────────────────────────────────
export const LS_OFFLINE_CACHE_PREFIX = 'mm_cache_';

// ── Site Settings (Admin) ────────────────────────────────────────────────────
export const LS_SITE_SETTINGS = 'siteSettings';

// ── Reading Progress ──────────────────────────────────────────────────────────
export const LS_READING_PROGRESS = 'mm_reading_progress';

// ── Stores ───────────────────────────────────────────────────────────────────
export const LS_GAMES_STORE = 'minesminis_games';
export const LS_WORDS_STORE = 'minesminis_words';
export const LS_WORKSHEETS_STORE = 'minesminis_worksheets';

// ── Cookie Consent (GDPR / KVKK) ─────────────────────────────────────────────
export const LS_COOKIE_CONSENT = 'mm_cookie_consent';
